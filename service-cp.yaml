apiVersion: v1
kind: Service
metadata:
  name: my-service1
  labels:
    app: revise1
    env: Dev1
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: revise1
    env: Dev1